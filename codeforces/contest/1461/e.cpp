#include <bits/stdc++.h>

using namespace std;

void solve() {
    ll k, l, r, t, x, y;
    cin >> k >> l >> r >> t >> x >> y;
}
int main() {
    ios::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL);

    int t = 1;
    cin >> t;
    for (; t--;) solve();

    return 0;
}
