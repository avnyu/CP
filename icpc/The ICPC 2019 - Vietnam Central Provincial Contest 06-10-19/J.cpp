#include<bits/stdc++.h>
#define ll long long

using namespace std;

const int N = 1e5 + 7;

ll p, q;
int n, k;
vector<int>a;

int main() {
	ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);

	cin >> n >> k;
	a.assign(n, 0);
	for (auto&i : a)cin >> i;

	

	return 0;
}